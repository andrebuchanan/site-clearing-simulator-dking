import UserControls from "../main/components/UserControls";

describe("handleClick", () => {

    test("When left button clicked, left command passed to callback", () => {
        fail;
    });

    test("When right button clicked, right command passed to callback", () => {
        fail;
    });

});

describe("handleSubmit", () => {

    test("When submitted, advance command passed to callback", () => {
        fail;
    });

    test("When submitted, correct advance value passed to callback", () => {
        fail;
    });

    test("When submitted, input box is reset to blank", () => {
        fail;
    });
});